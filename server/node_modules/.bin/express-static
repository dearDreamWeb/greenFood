#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_express-static@1.2.6@express-static/bin/server.js" "$@"
  ret=$?
else 
  node  "$basedir/../_express-static@1.2.6@express-static/bin/server.js" "$@"
  ret=$?
fi
exit $ret
